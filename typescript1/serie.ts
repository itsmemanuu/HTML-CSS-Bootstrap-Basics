export class Serie{
    constructor(public id: number, public nombre: string, public network: string, public temporadas: number, public descripcion: string, public streaming: string, public imagen: string){

    }
}